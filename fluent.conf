<source>
  @type forward
  port  24224
</source>

<match **>
  <store>
    type elasticsearch
    host ES_HOST
    port ES_PORT
    include_tag_key true
    logstash_format true
    flush_interval 10s
  </store>
</match>
